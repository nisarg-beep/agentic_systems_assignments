x = int(input("enter the first number:"))

if x == str:
    raise ValueError("invalid ")

y = int(input("enter the second number:"))

if y == str:
    raise ValueError("invalid")

sum = x + y 
print ("The sum of the given numbers is :" ,sum)


if y == 0:
    raise ZeroDivisionError("cannnot divide with zero")
div = x / y 
print (div )
